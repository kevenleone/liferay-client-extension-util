{
    "name": "@liferay-client-extension-util/fastify",
    "module": "index.ts",
    "type": "module",
    "devDependencies": {
        "@types/bun": "latest",
        "@types/jwk-to-pem": "^2.0.3",
        "@liferay-client-extension-util/auth": "workspace:*",
        "@liferay-client-extension-util/shared": "workspace:*"
    },
    "dependencies": {
        "fastify": "^5.4.0",
        "fastify-plugin": "^5.0.1",
        "jwk-to-pem": "^2.0.7"
    },
    "exports": {
        ".": "./dist/index.js"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "scripts": {
        "build": "bun build ./src/*.ts --outdir ./dist --target node --root ./src && tsc --emitDeclarationOnly --declaration --outDir ./dist ./src/index.ts > /dev/null 2>&1 || true"
    }
}
